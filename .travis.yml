language: python

python:
- "3.5"

dist: trusty
sudo: true

before_install:
- sudo apt-get install -y cmake git python3 python3-dev g++ wget python3-pip
- pip install numpy
- pip install llvmlite
- pip install numba

script: python hello.py